# TODO — Mobile Student Sidebar Dropdown + Brand Left Corner

## Steps
- [x] 1. Pin brand (logo + HTU SRC Feedback System) to the left corner in `mobile.css` (add `justify-content: space-between` on `.navbar-container` at ≤768px).
- [x] 2. Convert student sidebar into a mobile dropdown in `mobile.css` (collapsed header + `.open` reveal state, chevron, dark mode).
- [x] 3. Add a chevron indicator in the student sidebar header in `base.html`.
- [x] 4. Add `initMobileStudentDropdown()` toggle logic in `main.js` and register it in `DOMContentLoaded`.

## Status
All steps complete.
